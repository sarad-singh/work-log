<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Worklog - Admin Signin</title>
</head>
<style>
    body {
        margin: 0;
    }

    .main {
        display: flex;
        width: 100vw;
        height: 100vh;
    }

    .main .left {
        font-size: 50px;
        font-weight: bolder;
        display: flex;
        justify-content: center;
        align-items: center;
        width: 50%;
        background-color: #111;
        color: #fff;
    }

    .main .right {
        display: flex;
        align-items: center;
        padding: 20px;
        padding-left: 100px;
        width: 50%;
    }

    .input-element {
        margin-bottom: 32px;
    }

    .input-element input {
        border: none;
        border-bottom: 2px solid #111;
        margin-bottom: 10px;
        width: 300px;
        font-size: 18px;
        padding: 6px;
        box-sizing: border-box;
    }

    button[type=submit] {
        background-color: #111;
        border: 0;
        color: #fff;
        width: 300px;
        padding: 12px;
        font-size: 18px;
        cursor: pointer;
    }

    .action-feedbacks {
        margin: 12px 0;
    }

    .feedback.error {
        color: red;
    }

    .feedback.success {
        color: green;
    }

    .form-footer-guide {
        margin-top: 20px;
    }
</style>

<body>
    <div class="main">
        <div class="left">
            <h1>Work Log</h1>
        </div>
        <div class="right">
            <div class="form">
                <h1 class="title">Admin Signin</h1>
                <div class="action-feedbacks">
                    <div class="feedback error">{{errorMessage}}</div>
                    <div class="feedback success">{{successMessage}}</div>
                </div>

                <form action="/admin/auth/signin" method="POST">
                    <div class="input-element">
                        <input type="email" placeholder="email" name='email' value="{{data.email}}">
                        <div class="feedback error">{{errors.email}}</div>
                    </div>
                    <div class="input-element">
                        <input type="password" placeholder="password" name='password' value="{{data.password}}">
                        <div class="feedback error">{{errors.password}}</div>
                    </div>
                    <button type="submit">
                        Submit
                    </button>
                </form>
                <div class="form-footer-guide">
                    <div>Not an admin!!!. Here's Employee <a href="/employee/signin">Signin</a> Portal</div>
                </div>
            </div>
        </div>
    </div>
</body>

</html>